name: "Updated yml file"
permissions:
  contents: write
jobs:
  ansible:
    name: "Ansible content"
    runs-on: ubuntu-latest
    environment: production
    defaults:
      run:
        shell: bash
    steps:
      - name: Ansible code to run
        run: "example-machine-0.domain1.local example-machine-1.domain1.local example-machine-2.domain1.local example-machine-3.domain1.local example-machine-4.domain1.local"
        shell: bash
